# fake-org/skills

A test repository used by rosie's regression suite.

## Overview

This README is the content rosie installs when called with `--ref` (no
`--skill` flag), capturing the README at the repo root as a reference.
